-- InventoryController: This LocalScript runs on the player's computer (the "client").
-- It mirrors InventoryService on the server — the server tracks the real inventory,
-- but this controller displays it on screen: how much ore the player is carrying,
-- their bag capacity bar, and current ore values from the market.
-- For now it just confirms it loaded correctly.

print("[InventoryController] Loaded")
