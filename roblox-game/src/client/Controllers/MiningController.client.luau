-- MiningController: This LocalScript runs on the player's computer (the "client").
-- It mirrors OreService on the server — the server handles all real mining logic,
-- but this controller handles what the player SEES and HEARS when they mine:
-- swing animations, hit sound effects, floating damage numbers, and ore break VFX.
-- For now it just confirms it loaded correctly.

print("[MiningController] Loaded")
