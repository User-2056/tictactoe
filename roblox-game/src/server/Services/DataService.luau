-- DataService: This module will eventually handle saving and loading player data.
-- It will store each player's coins, inventory, equipment tier, and unlocked
-- regions in Roblox's DataStore so progress is kept between sessions.
-- It will also handle auto-saving every 60 seconds as a safety net.
local DataService = {}

print("[DataService] Loaded")

return DataService
