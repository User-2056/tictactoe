-- ContractService: This module will eventually manage mining contracts.
-- Contracts are optional missions (e.g. "Mine 30 Coal") that reward bonus coins.
-- It will generate a set of contracts for each player, track their progress,
-- and hand out rewards when a contract is completed.
local ContractService = {}

print("[ContractService] Loaded")

return ContractService
